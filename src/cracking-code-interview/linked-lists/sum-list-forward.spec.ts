import { SinglelyLinkedList } from './interfaces';
import { sumListsForwards } from './sum-list-forward';

describe(sumListsForwards.name, () => {
  it('should return 9->1->2', () => {
    /*const one: SinglelyLinkedList = {
      value: 7,
      next: {
        value: 1,
        next: {
          value: 6
        }
      }
    };
    const two: SinglelyLinkedList = {
      value: 5,
      next: {
        value: 9,
        next: {
          value: 2,
        }
      }
    };
    const result = sumListsForwards(one, two);
    console.log('result: ', JSON.stringify(result, null, 2));
    expect(result.value).toBe(9);
    expect(result.next.value).toBe(1);
    expect(result.next.next.value).toBe(2);
    */
   console.log(`sumListForward needs some love`);
  });
});