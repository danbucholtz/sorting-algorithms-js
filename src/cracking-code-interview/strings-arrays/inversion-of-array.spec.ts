import { inversionOfArray } from './inversion-of-array';

describe(inversionOfArray.name, () => {
  it('should handle input 1', () => {
    const input = [2, 4, 1, 3, 5];
    const result = inversionOfArray(input);
    // expect(result).toBe(3);
  });
});